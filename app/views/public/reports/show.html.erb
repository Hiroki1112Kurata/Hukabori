<h1>投稿詳細</h1>
<div>
  <div><%= attachment_image_tag @report, :image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg", size: "100x100" %></div>

  <p>タイトル：<%= @report.title %></p>
  <p>名前：<%= @report.user.first_name %></p>
  <p>作業内容・成果：<%= @report.content %></p>
  <p>考えたこと・学んだこと：<%= @report.learning %></p>
  <p>今後実施すること：<%= @report.action %></p>
  <p>管理者コメント：<%= @report.admin_comment %></p>
  <% if @report.user == current_user %>
    <%= link_to "編集", edit_report_path(@report.id) %>
    <%= link_to "削除", report_path, method: :delete, "data-confirm" => "本当に削除しますか？" %>
  <% end %>
</div>

<div>
  <p>コメント件数：<%= @report.report_comments.count %></p>
  <% @report.report_comments.each do |report_comment| %>
    <div>
     <%= report_comment.user.last_name %>
     <%= report_comment.comment %>
    </div>
  <% end %>
</div>

<div>
  <%= form_with(model:[@report, @report_comment],local: true) do |f| %>
  <%= f.text_field :comment, rows:'5',placeholder: "コメントをここに" %>
  <%= f.submit "送信"%>
  <% end %>
</div>
